import ButtonCounter from './ButtonCounter';

export default {
    title: 'components/atoms/ButtonCounter',
    component: ButtonCounter,
}

export const Default = {
    render: () => (
        <ButtonCounter />
    )
}